import { createTheme } from '@mui/material/styles';

export const darkTheme = createTheme({
    palette: {
        mode: 'dark',
        background: {
            //main: black[500]
        },
        backgroundDark: "#212121",
        container: "#424242"
    },

});